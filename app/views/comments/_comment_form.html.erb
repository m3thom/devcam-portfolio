<% unless current_user.is_a?(GuestUser) %>
    <%= form_for @comment, url: '#' do |f| %>
        <div>
            <%=f.label :content %>
            <%= f.text_area :content %>
        </div>

        <%= f.submit "Post comment"%>
    <% end %>
<% end %>